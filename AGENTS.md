# Guidelines

- Keep HTML, CSS (Tailwind classes) and JavaScript readable with 2-space indentation when editing files in this repository.
- Prefer descriptive element IDs and function names when modifying JavaScript.
- Avoid introducing external build steps; continue to rely on static assets/CDN scripts.

